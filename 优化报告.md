# dsh-installer 项目检查与 UI 优化报告

- 时间：2026-08-15
- 项目：`E:\Ai\tools\dsh-installer`（DeepSeek Harness 图形化管理器，C# WinForms）

---

## 一、项目现状

| 文件 | 说明 |
|---|---|
| `dsh-manager.cs` | 唯一源码（WinForms 管理器，当前 ~1600 行） |
| `DSH Manager.exe` | 管理器可执行文件（**唯一 exe，直接用这个**） |
| `plugin-server.js` | **新增**：插件服务器（node 单文件、零依赖），可独立部署 |
| `package.json` | 安装目录模板（`dsh web` 启动脚本 + `update` 更新脚本） |
| `app.manifest` | 新增：DPI 感知（PerMonitorV2） |
| `build.cmd` | 新增：一键编译脚本 |
| `优化报告.md` | 本报告 |

> 已清理：`DeepSeek Harness.exe`、`DSH.exe`、`DSH-Launcher.exe`、`DSH Manager.old.exe`
> 均为同一源码的更早 csc 构建产物（功能冗余、版本陈旧），已删除。需要旧版可
> 从回收站恢复，或随时用 `build.cmd` 重新编译当前源码。

## 二、审计发现与修复（代码级）

| # | 问题 | 状态 |
|---|---|---|
| 1 | `--launch` 参数被忽略：桌面快捷方式传了 `--launch`，但 `Main` 未读取 → 快捷方式形同虚设 | ✅ 已实现：`--launch` 自动「切到启动页 → 自动启动/打开浏览器」 |
| 2 | 停止逻辑 bug：清理循环检查 `proc.MainModule.FileName` 是否含 "dsh"/"bin.js"（那是 node.exe 路径，几乎永不匹配）→ 残留子进程清不掉 | ✅ 改为 `taskkill /PID <主进程> /T /F` 终止整棵进程树，再用 `netstat` 按端口定位残留 PID 逐一清理，**不会误杀其他 node 进程** |
| 3 | `Log()` 用硬编码控件索引 `Controls[6]` 定位日志框 | ✅ 改为成员字段直接引用；v2 中安装/启动日志合并为**同一个日志框**（`txtLog`） |
| 4 | 打开浏览器用 `lblUrl.Text.Replace("地址: ", "")` 取 URL | ✅ 改为 `currentUrl` 字段，端口变化时同步更新 |
| 5 | Node.js 版本硬编码 `v22.14.0` + 固定 MSI URL | ✅ 从 `nodejs.org/dist/index.json` 动态解析**最新 LTS**（WebClient + node 双通道），失败才回退 v22.14.0 |
| 6 | 安装写入的 package.json 硬编码 `^0.1.0-rc.6` → 新装用户装旧版 | ✅ 改为 `"@deepseek-ai/dsh": "latest"` |
| 7 | 更新比较是字符串 `!=` → 可能「降级」 | ✅ 改为简单 semver 比较，仅 `latest > local` 才更新 |
| 8 | `UseShellExecute=true` + 不重定向输出 → dsh 控制台日志不可见 | ✅ 改为重定向 stdout/stderr，`[dsh]` 前缀实时汇入日志面板 |
| 9 | 无单实例保护 | ✅ 命名 Mutex + 激活既有窗口 |
| 10 | `FixedSingle` 不可缩放；无 DPI 清单；状态栏静态"就绪"；无图标 | ✅ Sizable + MinimumSize；`app.manifest` PerMonitorV2；状态栏动态（就绪/运行中 vX/错误）；运行时绘制品牌蓝图标 |
| 11 | 无构建脚本 / 版本控制 | ✅ 新增 `build.cmd`；建议 `git init`（见下） |
| 12 | `GetPidsListeningOn`/`GetNodeVersion` 等读取 `StandardOutput` 但未设置重定向 → 实际恒抛异常（静默失效） | ✅ 新增 `StartProcCapture`（异步读 stdout+stderr 防管道阻塞），全部改用之 |
| 13 | 插件列表无来源 | ✅ 新增「插件」页（GitHub 实时 + 服务器代理 + 本地缓存），见下 |

> 说明：计划中"用 WMI 查命令行"的停止方案，实施时改为更轻量的 `taskkill /T`（原生进程树终止）+ `netstat` 端口反查 PID（无额外程序集依赖、无需管理员权限），效果等同且更安全。

## 三、UI 优化内容

- **品牌色**：主操作按钮（安装/启动/打开浏览器）改为品牌蓝 `#4176E6`（与 Web GUI 的 `--dsw-static-deepseek-500` 一致）+ 白字，带 hover/按下状态；次要按钮白色底 + 细边框 + 浅蓝 hover。
- **排版**：全局 Segoe UI 9pt；统一按钮高度与间距；安装页路径行改为可拉伸三列布局（缩放窗口时输入框自适应）；日志保持黑底绿字 Consolas 终端风。
- **窗口**：可缩放（Sizable）、可最大化、`MinimumSize(760, 620)`；标题栏/任务栏为品牌蓝圆角 + 白色 DSH 图标（运行时绘制，无需 .ico 文件）。
- **About 页**：精简文案 + 功能列表 + 当前已安装版本显示 + GitHub 链接可点击。
- **状态栏**：动态显示 就绪 / 运行中 v0.1.0-rc.6 / 已停止 / 错误 / 未安装。
- **DPI**：PerMonitorV2 感知（Win10 1703+），高分屏文字清晰；已确认 manifest 嵌入 PE 资源段。
- **应用窗口模式（不用浏览器标签页）**：新增「打开应用窗口」按钮与「用应用窗口打开（不弹浏览器标签页）」复选项（默认开启）。启动后自动用 **Edge/Chrome 的 `--app` 模式**打开一个没有标签页、没有地址栏的独立应用窗口（观感即桌面应用），找不到 Edge/Chrome 时回退到默认浏览器。

### 关于「内嵌 WebView2」的说明（当前不可行）

曾尝试把界面真正嵌入管理器窗口（WebView2 控件），结论如下：

- 本机已装 WebView2 Runtime（151.0.4129.78），但缺少 .NET 互操作程序集
  （`Microsoft.Web.WebView2.WinForms.dll` / 托管版 `Core.dll`）。
- 本机 Intel 显卡软件里虽有 `Core.dll` 与 `WebView2Loader.dll`，但均为**原生 x64
  版本**（无 .NET 元数据），无法被 .NET Framework 引用。
- 网络无法访问 nuget.org（TLS 被系统级拦截，curl 报 `SEC_E_NO_CREDENTIALS`），
  镜像源亦不可达，下载官方 NuGet 包失败。

**后续如能拿到官方包**（在可联网的机器下载
`Microsoft.Web.WebView2` NuGet 包，把 `Microsoft.Web.WebView2.Core.dll`、
`Microsoft.Web.WebView2.WinForms.dll`、`WebView2Loader.dll`（x64）放进
`dsh-installer` 目录），即可把应用窗口升级为真正内嵌的 WebView2 窗口。

## 三之补充：v2 更新（标签页整合 + 插件中心 + 服务器版）

### 1. 标签页整合

「安装」+「启动」合并为 **「管理」** 页：安装目录 → 状态/地址/端口 → 操作按钮
（安装/启动/停止/应用窗口打开/浏览器打开）→ 选项（自动更新/自动打开/应用窗口）
→ 进度条 → 日志（安装与启动共用）。窗口内三个页签：**管理 ｜ 插件 ｜ 关于**。

### 2. 插件中心（「插件」页）

- 数据源：GitHub `topic:dsh-plugin`（实测 **3400+ 仓库**，取最近更新的前 300 个）。
- 自动分类（关键词启发式）：界面主题 / 身份认证 / 计费支付 / 视觉多模态 /
  智能体协作 / 网关与集成 / 工具命令 / 合集资源 / 其他。
- 列表：名称/分类/简介/⭐/最近更新，按分类分组，分类下拉过滤，点列头排序，
  双击打开仓库，**「下载 ZIP」** 保存仓库压缩包。
- 实时性：启动时后台预取 + **每 30 分钟自动刷新** + 手动「刷新」（`?force=1`）。
- 离线兜底：结果缓存到 `%APPDATA%\DeepSeek-Harness-Manager\plugins-cache.json`，
  断网/服务器不可达时显示缓存并提示。

### 3. 插件服务器（无 VPN 方案，`plugin-server.js`）

| 端点 | 说明 |
|---|---|
| `GET /api/plugins` | 服务器侧抓 GitHub（3 页）→ 分类 → 内存+磁盘缓存（30 分钟 TTL，`plugins-cache.json` 同目录）→ JSON |
| `GET /api/plugins/zip?repo=owner/name` | **代理下载**仓库 ZIP（查默认分支 → 流式转发 codeload） |
| `GET /` | **浏览器版插件中心**（纯 HTML+JS，无需 VPN，分享网址即可给别人用） |
| `GET /api/health` | 存活检查 |

- 零依赖（仅 node 内置模块），部署：`node plugin-server.js --port 8080`；
  服务器机器需能访问 GitHub（境外 VPS 直接可用；国内服务器可设
  `HTTPS_PROXY=http://127.0.0.1:7897`，内置 CONNECT 隧道支持）。
- 客户端「插件」页顶部填服务器地址（存入设置）后，**列表与 ZIP 下载全部走服务器**，
  客户端无需 VPN。留空则直连 GitHub（node → 本地代理 → WebClient 级联）。

### 4. 无 VPN 三链路（重要）

| 需求 | 无 VPN 路径 | 结论 |
|---|---|---|
| 插件列表/分类 | 客户端 → 插件服务器（有外网的机器） | ✅ 服务器可达即可 |
| 插件 npm 安装（`dsh plugin add`） | npm 走 **npmmirror 镜像**（国内可达） | ✅ 本来就不需要 VPN |
| 插件 GitHub 仓库 ZIP | 客户端 → 服务器 `/api/plugins/zip` 代理 | ✅ 服务器代理 |

> 前提：插件服务器必须部署在**能访问 GitHub** 的机器上（境外 VPS 或开代理的国内
> 服务器），客户端只要能访问该服务器即可。

### 5. 网络调查结论（本机）

- 本机 **schannel（Windows TLS 栈）损坏**：WebClient/curl/Invoke-WebRequest 对所有
  站点报 `SEC_E_NO_CREDENTIALS`；**node 的 OpenSSL 正常** → 本方案取数优先走 node。
- 本机装有 **Clash Verge**（verge-mihomo 监听 `127.0.0.1:7897`），DNS 解析 GitHub
  为 fake-IP（198.18.0.x）。管理器自动探测该代理端口。
- 已实测：经代理 + node 抓 GitHub 成功（HTTP 200，3443+ 仓库）；管理器端到端
  （设置 → 服务器拉取 → 分类 → 缓存写入）验证通过。

## 四、构建方法

```bat
build.cmd
```
（调用系统自带 `csc.exe`，输出 `DSH Manager.exe`，无需安装任何 SDK / NuGet 依赖。）

## 五、验收清单（建议手动过一遍）

- [ ] 启动管理器 → 状态栏显示「运行中 v…」（本机 3080 已有服务）；三个页签：管理/插件/关于
- [ ] 「管理」页：修改端口 → 地址标签即时跟随
- [ ] 「管理」页：勾选「用应用窗口打开」→ 点「启动」→ 弹出无标签页的应用窗口（Edge --app）
- [ ] 「应用窗口打开」按钮 → 再次打开应用窗口；「浏览器打开」→ 默认浏览器标签页
- [ ] 「停止」→ 服务停止且无 node 残留进程；日志面板实时滚动 `[dsh]` 输出
- [ ] 「插件」页：首次打开自动拉取 → 列表出现分类分组数据（本机 VPN 开启时）
- [ ] 「插件」页：分类下拉过滤、点列头排序、双击行打开仓库
- [ ] 「插件」页：填 `http://127.0.0.1:8080`（本地起 `node plugin-server.js`）→ 走服务器取数
- [ ] 「插件」页：选一个插件「下载 ZIP」→ 保存为 .zip 且可解压
- [ ] 断网/关服务器 → 插件页显示本地缓存 + 提示，不崩溃
- [ ] 双击桌面快捷方式（`--launch`）→ 自动打开界面
- [ ] 再次启动管理器 → 只激活已有窗口（单实例）
- [ ] 高分屏（缩放 >100%）文字清晰
- [ ] 安装流程（如需要）：选目录 → 安装 → 建快捷方式

## 五·五、插件服务器部署文档

```bash
# 1) 有 node 且能访问 GitHub 的机器（境外 VPS 最佳）：
node plugin-server.js --port 8080

# 2) 国内服务器 + Clash 类代理：
HTTPS_PROXY=http://127.0.0.1:7897 node plugin-server.js --port 8080

# 3) 常驻（二选一）：
#    pm2 start plugin-server.js -- --port 8080
#    systemd: ExecStart=/usr/bin/node /opt/dsh/plugin-server.js --port 8080

# 4) 防火墙放行端口；客户端「插件」页填 http://<服务器IP>:8080
```

- 浏览器直接访问 `http://<服务器IP>:8080` 即为插件中心网页（可分享给别人）。
- 缓存文件 `plugins-cache.json` 与服务器同目录，30 分钟 TTL，重启后仍可用。

## 五·六、v3 更新（美化中心页 + 下载链路修复）

### 1. 「美化」页（新页签）

- 界面单独一页「美化」，专门收录**前端美化类插件**（二次元 / 换肤 / 赛博朋克 /
  玻璃质感 / 怀旧复古），数据源为 GitHub 多关键词搜索（9 组查询：
  `topic:dsh-plugin` × theme/skin/anime/miku/cyberpunk/retro/二次元/glass/ui，
  按 ⭐ 排序合并去重），实测抓到 **93 个**美化插件。
- 列表 6 列：名称 / 风格 / 简介 / ⭐ / 最近更新 / 状态；按风格下拉过滤、点列头排序。
- 操作：**下载 ZIP** → **启用**（装入本地 DSH profile）→ **停用**（移除还原）→ **打开仓库**。
- 风格自动分类（关键词启发式）：二次元动漫（miku/初音/touhou/灵梦/原神/genshin/
  waifu/vocaloid…）、赛博朋克（cyber/霓虹/night city）、怀旧复古（qq2006/2007/
  retro/win95…）、玻璃质感（glass/frosted/毛玻璃/transparent…）、换肤系统
  （skin/theme/皮肤/主题…）。已剔除误伤词（pixel/卡通/moe 等），避免把
  视觉类工具误判为美化。
- 状态持久化：`%APPDATA%\DeepSeek-Harness-Manager\beautify.json`（已下载/已启用）；
  列表缓存 `beautify-cache.json`（30 分钟 TTL，离线回退）。

### 2. 「启用/停用」的准确机制（本次关键修正）

经 dsh 源码核实（`dsh-app-boot` + `dsh/lib/plugin-*.js`）：

- `dsh plugin add <pkg>` = pnpm add + **reconcile**：把「package.json 声明了
  `dsh.bundle.patch` 的依赖」自动追加进 profile 的 `dsh.profile.bundles` 层列表。
- **dependencies 本身不参与加载**；bundles 列表里的包才会被 cordis 装载
  （读其 `dsh.bundle.patch` → 插件配置树）。美化插件的包都是 bundle 格式
  （如 `@deepseek-ai/dsh-client-ui-skin-miku` 带 `dsh.bundle.patch`）。
- 管理器启用 = 复制插件到 `profiles/web/node_modules/<包名>` + 复刻 reconcile
  （写 dependencies + 检查 `dsh.bundle` 后追加 bundles）；停用 = 移除
  dependencies 条目 + 从 bundles 移除 + 删目录。每次操作前自动备份
  `package.json` 等到 `~/.dsh-beautify/backups/<时间戳>/`。
- **启用后需重启 DSH 服务（dsh web）生效**，界面已明确提示。

### 3. 下载链路修复（重要 bug）

- 原 ZIP 下载脚本用 `fetch`（undici）：**codeload 分块传输时 body 挂起**
  （直连与代理均复现，90~150s 超时无响应）。
- 已重写为**原生 `https.request` + `http.request` CONNECT 隧道**
  （与 plugin-server.js 同款已验证实现，node 自动解析 CONNECT 响应，无混包问题）。
- 实测：走 Clash 代理下载 dsh-miku-skin ZIP **5.4MB / 14.5s / PK 头正确**。
- 端到端实测（临时 DSH_HOME 模拟 profile）：启用 → `dsh --profile web
  --dump-config` 出现 `id: ui-skin-miku`（插件进入配置树）→ 停用 → 配置树干净。

### 4. 待办（阶段 2：服务器，等待用户确认后执行）

- 服务器备份 → nvm 装 node v22 → `/opt/dsh/` 部署 plugin-server + DSH（systemd）
- 宝塔新站 `plugins.falseai.cn`（反代 127.0.0.1:8080 + SSL）、`dsh.falseai.cn`
  （反代 127.0.0.1:3080 + SSL + WebSocket）
- PWA（manifest + sw.js，nginx 放行）实现「app 模式」
- ai.falseai.cn 用户体系与 DSH 打通（PHP 认证端点 + nginx `auth_request`，不动 DSH 核心）
- SQLite → MariaDB 迁移（趁用户量小，11 表，bootstrap.php 支持配置切换）

## 五·五、v4 本地稳定性审计与修复（2026-08-16）

背景：用户反馈「明明已启用的 miku 美化，管理器却不显示」「启用其他美化没反应」。
本次对 `dsh-manager.cs`（~3160 行）做了全文件审计（对照 DSH 实装源码核实装载机制），
修复了 P0~P3 共 4 级问题并重做美化页交互。备份见 `backups\2026-08-16\`。

### 1. 核心根因与修复

| # | 级别 | 问题 | 修复 |
|---|---|---|---|
| 1 | P0 | 启用从不安装插件依赖、无装载预检 → 带依赖的皮肤下次 `dsh web` 启动 fail-loud 砸掉整个界面 | 启用前在缓存目录跑 `npm install --production`（缺依赖时）；启用后执行 `dsh --profile web --dump-config` 预检（exit≠0 / cannot resolve 即失败）；**失败自动回滚**（还原 manifest 备份 + 删新目录） |
| 2 | P0 | 停止按钮按端口 taskkill 无辜进程；端口被非 DSH 占用时误显示「运行中」 | 只杀自己启动的 `dshProcess` 树；端口残留 PID 用 Win32_Process 命令行校验（含 bin.js 才杀）；CheckInstallation/RunStart 区分「DSH 运行中」vs「端口被其他程序占用」（后者禁用启动/停止） |
| 3 | P0 | 启用/停用直接删/覆盖 `node_modules/<pkg>`，可毁掉 DSH 内装包 | 删除前归属校验：与 DSH 内置包冲突 / 目标目录非本管理器安装 → 拒绝；删除一律改为**移到备份目录**（可恢复） |
| 4 | P0 | ZIP 解压 `ExtractToDirectory` 无条目校验（Zip Slip） | 改 `SafeExtractZip`：逐条拒绝绝对路径 / `..` / 盘符 / 越界条目；下载后校验 ZIP 魔数 `PK\x03\x04` |
| 5 | P1 | 服务器 ZIP 下载的 node 回退用文本脚本写二进制 → 必然损坏 | 改用二进制安全 `SCRIPT_DOWNLOAD_FILE`（CONNECT 隧道）+ 魔数校验 |
| 6 | P1 | 状态与真实 profile 永不二次同步（手动 dsh plugin / 删目录后 UI 撒谎） | 新增 `ReconcileBeautyState`：enabled 必须同时满足 bundles 含包名 + 目录存在，否则降级「已下载」；启动时自动对账 |
| 7 | P1 | 启用/停用无互斥、`beautyState` 字典跨线程读写 | `SemaphoreSlim(1,1)` 串行化 + `ConcurrentDictionary` + busy 标志 |
| 8 | P1 | 后台操作中关窗体 → async 延续访问已释放控件崩溃 | 所有 `await` 延续开头 `IsDisposed` 检查 |
| 9 | P2 | 美化页忽略已配置的插件服务器（无 VPN 用户浏览不了） | `FetchBeautifyList` 优先走服务器 `/api/plugins` 过滤美化，失败才回退 GitHub 多关键词搜索 |
| 10 | P2 | `CheckInstallation` 静默改写已保存的 installDir | 自动探测不再写设置；仅浏览/安装等显式动作保存 |
| 11 | P2 | 同名包名两个仓库互相覆盖 | 启用前拒绝：包名已被其他仓库占用 |
| 12 | P2 | 状态文本互相覆盖（进度文案被计数冲掉） | `beautyBusy` 标志：操作在途时不覆盖状态文案 |
| 13 | P2 | 遗留数据：enabled 但 PkgName 为空 → 显示已启用却拒绝停用 | LoadBeautyState 丢弃此类条目 |
| 14 | P3 | IPv6 监听漏检（`[::]:port`） | netstat 正则支持 `\[.*\]` |
| 15 | P3 | `RunStart` 后台线程改进程级 `Environment.CurrentDirectory` | 移除，仅用 `WorkingDirectory` |
| 16 | P3 | `FindPluginRoot` 可能选 example/ 子目录 | 优先检查解压根目录本身 |
| 17 | P3 | `IsSafePkgName` 允许 node_modules/package.json 等名 | 拒绝保留段名 |
| 18 | P3 | 登记失败残留孤儿目录 | 统一 `RollbackProfile` 回滚 |
| 19 | P3 | 关于页版本号只在构造时算一次 | 字段化 + 安装完成后刷新 |

### 2. 皮肤互斥（用户已确认方案）

启用新皮肤时：先预检新皮肤，成功后**同一事务内自动停用旧皮肤**（bundles + dependencies 移除，
源码保留为「已下载」，可一键切回）。判断依据：`skin.json` 存在或 `cordis.patch.yml` 含 `ui-skin` 特征。
壁纸、宠物等非皮肤插件不受影响，仍可共存。

### 3. 美化页 UI 重做

- **本地状态优先**：真实 profile 里已启用/已下载的插件**固定合并进列表**（即使 GitHub 目录缺失
  也显示，修复「已启用却不显示」根因），且**已启用置顶**、整行浅绿。
- 底部摘要条动态显示：`启用中: X 个 · 已下载: Y 个 · 数据源: 插件服务器/GitHub/本地` + 颜色图例。
- 工具栏改为固定操作区 + 筛选区，状态筛选仍为「全部 / 已下载 / 未下载」（默认已下载）。
- 管理页新增「端口被其他程序占用（非 DSH 服务）」状态，避免误报运行中/误杀。

### 4. 验证记录（临时 DSH_HOME，未动真实 profile 与 3080 服务）

- SCRIPT_EDIT_PLUGIN 等价链路：bundle 判定正确、非 bundle 只登记依赖不追加 bundles、remove 幂等 —— 全部 PASS
- 预检：好皮肤 → `dump-config` exit 0 且配置树出现 `id: ui-skin-test2`；坏插件（目录缺失）→ exit 1 被拦截 —— PASS
- 皮肤互斥模拟：启用 B 后停用 A，A 从 bundles+deps 移除、B 保留 —— PASS
- 插件服务器新端点 `/api/beautify`：本地起服实测 total=17、类别正确、含 skin 类 —— PASS
- `build.cmd` exit 0；exe 启动存活冒烟 —— PASS
- 真实 profile（miku 已启用）只读核对无改动；3080 服务未受影响

### 5. 追加修复（2026-08-16 第二轮）

- 修复美化页服务器路径解析：`FetchBeautifyList` 原用 `ParseGitHubFile` 解析服务器返回
  （结构不兼容会静默失败回退）→ 改用 `ParsePluginList`，并优先请求服务器新端点
  `/api/beautify`（服务端已按与客户端一致的关键词过滤，旧版服务器自动回退 `/api/plugins`）。
- `plugin-server.js` 新增 `GET /api/beautify`（美化过滤 + 沿用 30 分钟缓存/stale 回退），
  与 `/api/plugins` 共用 `getPlugins` 数据链路。
- `RunNpmInstall` 加 10 分钟超时 + 超时杀整棵进程树（原无超时可能永久挂起）。
- `RunNodeScript` / `StartProcCapture` 超时处同样改为 `taskkill /T` 杀整棵进程树（原只 Kill 父进程，npm/cmd 子进程可能残留）。
- 美化缓存改为**合并写入**（`MergeBeautifyCaches`）：新搜索结果与旧缓存按 fullName 去重合并，
  搜索波动/部分查询失败不再让旧条目消失（配合本地状态合并，双重保证"已用插件不丢"）。

### 6. 修复"每次启动都更新 rc.6 → rc.7"的循环（用户反馈，2026-08-16）

- **根因**：`installDir/package-lock.json` 把 `@deepseek-ai/dsh` 锁在 `0.1.0-rc.6`，
  而管理器用不带包名的 `npm install` 更新——npm 按 lock 固定版本安装**不会升级**，
  退出码仍是 0 → 界面谎报「更新成功」，本地实际版本不变 → 下次启动又检测到 rc.7 → 无限循环。
- **修复**：
  - `RunNpmInstall` 改为显式 `npm install @deepseek-ai/dsh@latest --save`（绕过 lock 锁定，
    安装即升级并同步 package.json/lock）；
  - 更新后**回读本地版本校验**：版本确实变大才报「已更新到 X」，否则提示可能被 lock 锁定并给出手动命令；
  - `CompareVersions` 预发布段改为点分段数值比较（修复 `rc.9 > rc.10` 的字符串误判隐患），
    7 组用例全部 PASS。

### 7. 修复"更新时界面卡住无反馈"（用户反馈，2026-08-16）

- **根因**：`npm install @deepseek-ai/dsh@latest --save` 需下载大包（可能数分钟），
  而 `RunNpmInstall` 用不重定向输出的 `StartProc` 运行 → **日志区全程无输出**，用户看到
  "发现新版本"后就再无动静，误以为卡死（期间点"停止"也不会取消后台更新线程）。
- **修复**：
  - `RunNpmInstall` 改为重定向输出，npm 进度以 `[npm]` 前缀**实时流入日志面板**；
  - 更新前明确提示"正在下载新版（大包可能需要几分钟，下方 [npm] 日志实时显示进度）"；
  - `Log()` 补 `IsDisposed` 防护（下载期间关闭窗体不再抛 ObjectDisposedException）；
  - 10 分钟超时自动放弃并继续启动，不会永久卡死。

### 8. v5：修复新版 DSH 打开后要求输入「配对码」（用户反馈，2026-09-07）

- **背景**：升级到新版 dsh（0.1.1+，如 rc.9 系）后，管理器启动 Web UI 再打开界面，
  浏览器弹出「DSH 配对 / 请输入配对码」窗口，需要手动去终端抄 token。
- **根因**（已从官方 npm 包源码 `dsh-web-app` / `dsh-client-connection` 证实）：
  - 官方为修复安全漏洞（旧版 dsh web 无任何认证，局域网任何人可访问，见 GitHub
    Discussion #2829），在 0.1.1+ 内置了浏览器配对认证（BrowserAuth）。
  - dsh 启动时会向终端打印一行带令牌的地址，形如
    `dsh web: http://127.0.0.1:3080/?token=xxxx`（可能附 LAN 地址）。
    该 token 是**进程级一次性启动令牌**：浏览器带 `?token=` 访问时服务端校验通过，
    下发签名 cookie（默认 30 天有效）并 303 重定向到干净地址；无 token 且无 cookie
    则返回 401，前端显示配对码弹窗。
  - 管理器一直以 `web --port <port> --no-open` 启动、随后自己打开**裸地址**
    （`http://127.0.0.1:3080`，不带 token），因此新版必然触发配对码弹窗。
- **修复**：
  - 新增 `HandleDshOutput()`：dsh 的 stdout/stderr 每行先经过它，用正则
    `https?://[^\s]*[?&]token=[A-Za-z0-9_\-\.]+` 捕获带令牌 URL
    （优先回环地址，LAN 地址兜底），存入 `authenticatedUrl`；
  - 所有打开动作（应用窗口 / 浏览器 / 点击地址链接 / 启动后自动打开）统一走
    `GetOpenUrl()`，令牌就绪即带令牌打开，**全程无需手动输入配对码**；
  - 等待启动逻辑改为「端口就绪 + 令牌捕获」双条件：端口起来后最多再等 8 秒令牌
    （兼容 rc.8 及以前无令牌机制的旧版，不长等），自动打开前再兜底等 3 秒；
  - **日志打码**：令牌在日志面板显示为 `token=****（已隐藏）`，避免截图/分享泄露；
  - 新增令牌状态徽标（地址栏右侧）：🔒 待启动 / 🔒 启动中… / 🔓 免配对码（绿色）/
    🔒 外部实例、🔒 见日志、🔒 错误（橙色/红色）；
  - 新增「复制访问链接」按钮（令牌就绪后可用），一键复制带令牌 URL；
  - 地址由普通 Label 改为 **LinkLabel**，点击直接打开；令牌就绪后显示「✓已带令牌」；
  - 外部实例（端口被非管理器启动的 dsh 占用）拿不到令牌：徽标显示「🔒 外部实例」，
    日志提示「停止后由本管理器启动即可自动免配对码」，复制按钮保持禁用；
  - 停止 / 进程退出 / 异常分支均复位令牌状态与按钮。
- **界面优化**：
  - 窗口默认尺寸 780×660 → **860×700**，最小尺寸 760×620 → 820×640，管理页行高微调；
  - 选项区新增提示语「💡 新版 DSH 启动会自动获取访问令牌，打开界面无需手动输入配对码」；
  - 「自动打开」复选框文案改为「启动后自动打开界面」；关于页功能列表同步更新；
  - Edge/Chrome 应用窗口参数加 `--new-window`，避免与已有浏览器实例合并导致
    `--app` 独立窗口失效；打开成功/失败均写日志。
- **构建修复**：`build.cmd` 增加 `/codepage:65001`，强制 csc 按 UTF-8 读取源码
  （源码为 UTF-8 无 BOM 且含中文/emoji，否则中文注释与界面文字可能乱码）。
- **验证**：`build.cmd` 编译通过（exit 0），旧源码与脚本已备份至
  `backups\2026-09-07\`；实机免配对码效果待启动新版 dsh 确认。

## 六、回滚

- 旧版 exe 已删除：需要时可从回收站恢复，或直接用 `build.cmd` 从当前源码重新编译。
- 源码改动全部集中在 `dsh-manager.cs`，对照本报告即可还原行为。

## 七、后续建议（未实施）

1. **`git init` + 首次提交**：目前无版本控制，建议为当前干净状态做一次初始提交，之后每次改动可追溯。
2. **深色模式**：如需整窗深色主题（日志区已是黑底），可在现有配色结构上扩展。
3. **托盘驻留**：最小化到系统托盘常驻，可选。
4. **exe 文件版本信息**（FileVersion/ProductName）：需要额外的资源文件编译，属可选增强。
